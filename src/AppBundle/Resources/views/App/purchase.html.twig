{% extends "::base.html.twig" %}

{% block title %}AppBundle:App:purchase{% endblock %}

{% block body %}

    <h1>{{ message }}</h1>

    <a href="{{ path("home") }}" class="btn btn-success">
        <span class="glyphicon glyphicon-ok"></span> OK
    </a>

{% endblock %}